name = 'sample_decrypter'


def decrypt(clipboard_content):
    """/*
       Function: decrypt

         try decrypt clipbaord content

         (main program will call the function automatically if the filename match the expected hash. )

       Parameters:

         clipboard_content - clipboard string.

       Returns:

         decrypted string.
    */"""
    content = clipboard_content

    return content
